{% extends 'base_admin.html.twig' %}

{% block title %}Editer Panier{% endblock %}

{% block body %}
<div class="row">
    <div class="col-12 col-md-8 offset-md-2">

        <h1 class="admin-title text-center">Editer Panier <span class="admin-nom">{{ nom }}</span></h1>
        
        {{ include('panier/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
        
        <div class="my-2">
            <a class="btn btn-primary" href="{{ path('admin_paniers') }}">Retourner à la liste</a>
        </div>
        
        {{ include('panier/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
